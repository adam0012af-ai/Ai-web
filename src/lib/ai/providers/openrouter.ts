import type { AIProviderClient,AIRequest } from '../types';import { AIProviderError } from '../errors';
export const openrouterProvider:AIProviderClient={provider:'OPENROUTER',isConfigured:()=>!!process.env.OPENROUTER_API_KEY,async generate(req:AIRequest,model:string,signal:AbortSignal){const res=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',signal,headers:{'content-type':'application/json','authorization':`Bearer ${process.env.OPENROUTER_API_KEY}`,'HTTP-Referer':process.env.OPENROUTER_APP_URL??process.env.APP_URL??(process.env.VERCEL_PROJECT_PRODUCTION_URL?`https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}`:'http://localhost:3000'),'X-Title':process.env.OPENROUTER_APP_NAME??'Nexa AI'},body:JSON.stringify({model,messages:req.messages,temperature:req.temperature??0.5,max_tokens:req.maxTokens??1600})});if(!res.ok){const body=await res.text();throw new AIProviderError(`OpenRouter ${res.status}: ${body.slice(0,300)}`,`OPENROUTER_${res.status}`,res.status===429||res.status>=500,res.status);}const data=await res.json();const text=data.choices?.[0]?.message?.content??'';if(!text)throw new AIProviderError('OpenRouter returned no text','OPENROUTER_EMPTY',true);return{text,inputTokens:data.usage?.prompt_tokens??0,outputTokens:data.usage?.completion_tokens??0};}};
