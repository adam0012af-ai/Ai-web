import type { AIProviderClient,AIRequest } from '../types';import { AIProviderError } from '../errors';
export const geminiProvider:AIProviderClient={provider:'GEMINI',isConfigured:()=>!!process.env.GEMINI_API_KEY,async generate(req:AIRequest,model:string,signal:AbortSignal){const system=req.messages.find(m=>m.role==='system')?.content;const contents=req.messages.filter(m=>m.role!=='system').map(m=>({role:m.role==='assistant'?'model':'user',parts:[{text:m.content}]}));const res=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent?key=${encodeURIComponent(process.env.GEMINI_API_KEY??'')}`,{method:'POST',signal,headers:{'content-type':'application/json'},body:JSON.stringify({contents,systemInstruction:system?{parts:[{text:system}]}:undefined,generationConfig:{temperature:req.temperature??0.5,maxOutputTokens:req.maxTokens??1600}})});if(!res.ok){const body=await res.text();throw new AIProviderError(`Gemini ${res.status}: ${body.slice(0,300)}`,`GEMINI_${res.status}`,res.status===429||res.status>=500,res.status);}const data=await res.json();const text=data.candidates?.[0]?.content?.parts?.map((p:{text?:string})=>p.text??'').join('')??'';if(!text)throw new AIProviderError('Gemini returned no text','GEMINI_EMPTY',true);return{text,inputTokens:data.usageMetadata?.promptTokenCount??0,outputTokens:data.usageMetadata?.candidatesTokenCount??0};}};
