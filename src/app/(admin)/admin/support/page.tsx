import Link from 'next/link';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';import { Badge } from '@/components/ui/badge';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.supportTicket.findMany({include:{user:{select:{name:true,email:true}}},orderBy:{updatedAt:'desc'},take:100});return <><PageHeader title="Support queue" description="Open and historical customer support tickets."/><Card className="divide-y divide-[var(--line)]">{rows.map(x=><div key={x.id} className="p-4"><div className="flex flex-wrap justify-between gap-2"><div><Link className="font-bold hover:text-[var(--brand)]" href={`/admin/support/${x.id}`}>{x.subject}</Link><div className="muted text-xs">{x.user.name} · {x.user.email}</div></div><div className="flex gap-2"><Badge>{x.priority}</Badge><Badge>{x.status}</Badge></div></div><p className="muted mt-2 text-sm">{x.description}</p></div>)}</Card></>}