import { db } from '@/lib/db';import { getCurrentUser } from '@/lib/auth/session';import { PageHeader } from '@/components/dashboard/page-header';import { ProviderTable } from '@/components/admin/provider-table';export const dynamic='force-dynamic';export default async function Page(){const [rows,u]=await Promise.all([db.aIProviderConfig.findMany({orderBy:{priority:'asc'}}),getCurrentUser()]);return <><PageHeader eyebrow="AI ADMIN" title="AI providers" description="Enable providers, review health, and change fallback priority. Priority editing is restricted to Super Admin."/><ProviderTable canEdit={u?.role==='SUPER_ADMIN'} rows={rows.map(x=>({...x,lastError:x.lastError??null}))}/></>}