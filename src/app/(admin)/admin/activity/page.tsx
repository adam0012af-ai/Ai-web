import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.activityLog.findMany({include:{user:{select:{email:true}}},orderBy:{createdAt:'desc'},take:200});return <><PageHeader title="Activity log" description="Security and operational actions across the platform."/><Card className="divide-y divide-[var(--line)]">{rows.map(x=><div key={x.id} className="flex flex-wrap justify-between gap-3 p-4 text-sm"><span><b>{x.action}</b> <span className="muted">{x.user?.email??'System'}</span></span><span className="muted">{x.createdAt.toLocaleString()}</span></div>)}</Card></>}