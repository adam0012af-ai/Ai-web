import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { StatCard } from '@/components/dashboard/stat-card';export const dynamic='force-dynamic';export default async function Page(){const [users,convs,tickets,posts]=await Promise.all([db.user.count(),db.conversation.count(),db.supportTicket.count(),db.blogPost.count({where:{published:true}})]);return <><PageHeader title="Analytics" description="High-level product adoption counters. Add your analytics warehouse for deeper funnels and retention."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Users" value={users}/><StatCard label="Conversations" value={convs}/><StatCard label="Support tickets" value={tickets}/><StatCard label="Published posts" value={posts}/></div></>}