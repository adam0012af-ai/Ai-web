import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';import { PlanTable } from '@/components/admin/plan-table';export const dynamic='force-dynamic';export default async function Page(){const [rows,plans]=await Promise.all([db.subscription.findMany({include:{user:{select:{email:true}},plan:true},orderBy:{createdAt:'desc'},take:100}),db.plan.findMany({orderBy:{priceMonthly:'asc'}})]);return <><PageHeader title="Subscriptions & plan limits" description="Plan assignments plus editable AI and upload limits stored in the database."/><Card className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b border-[var(--line)]"><th className="p-4">User</th><th>Plan</th><th>Status</th><th>Started</th></tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-b border-[var(--line)] last:border-0"><td className="p-4">{x.user.email}</td><td>{x.plan.name}</td><td>{x.status}</td><td>{x.startsAt.toLocaleDateString()}</td></tr>)}</tbody></table></Card><PlanTable plans={plans}/></>}