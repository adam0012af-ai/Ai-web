import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.category.findMany({include:{_count:{select:{posts:true}}},orderBy:{name:'asc'}});return <><PageHeader title="Categories" description="Blog content taxonomy."/><Card className="divide-y divide-[var(--line)]">{rows.map(x=><div className="flex justify-between p-4 text-sm" key={x.id}><b>{x.name}</b><span className="muted">{x._count.posts} posts</span></div>)}</Card></>}