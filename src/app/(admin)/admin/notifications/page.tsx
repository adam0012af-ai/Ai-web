import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.notification.findMany({include:{user:{select:{email:true}}},orderBy:{createdAt:'desc'},take:100});return <><PageHeader title="Notifications" description="Latest user notification records."/><Card className="divide-y divide-[var(--line)]">{rows.map(x=><div key={x.id} className="p-4 text-sm"><b>{x.title}</b><span className="muted ml-2 text-xs">{x.user.email} · {x.type}</span><p className="muted mt-1">{x.body}</p></div>)}</Card></>}