import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.invoice.findMany({include:{user:{select:{email:true}}},orderBy:{issuedAt:'desc'},take:100});return <><PageHeader title="Invoices" description="Invoice records ready for payment-provider synchronization."/><Card className="divide-y divide-[var(--line)]">{rows.length?rows.map(x=><div key={x.id} className="flex flex-wrap justify-between gap-3 p-4 text-sm"><span>{x.user.email}</span><b>{(x.amount/100).toFixed(2)} {x.currency} · {x.status}</b></div>):<div className="muted p-8 text-center">No invoices yet.</div>}</Card></>}