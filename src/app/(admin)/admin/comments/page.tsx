import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.comment.findMany({include:{post:{select:{title:true}}},orderBy:{createdAt:'desc'},take:100});return <><PageHeader title="Comments" description="Moderation queue for blog comments."/><Card className="divide-y divide-[var(--line)]">{rows.length?rows.map(x=><div key={x.id} className="p-4 text-sm"><div className="flex justify-between"><b>{x.authorName}</b><span>{x.approved?'Approved':'Pending'}</span></div><div className="muted mt-1">On {x.post.title}</div><p className="mt-2">{x.content}</p></div>):<div className="muted p-8 text-center">No comments yet.</div>}</Card></>}