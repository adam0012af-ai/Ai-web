import { redirect } from 'next/navigation';import { getCurrentUser } from '@/lib/auth/session';import { AdminShell } from '@/components/admin/admin-shell';export default async function Layout({children}:{children:React.ReactNode}){const u=await getCurrentUser();if(!u)redirect('/login');if(!['ADMIN','SUPER_ADMIN'].includes(u.role))redirect('/dashboard');return <AdminShell>{children}</AdminShell>}