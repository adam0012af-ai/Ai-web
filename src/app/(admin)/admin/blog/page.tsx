import Link from 'next/link';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const rows=await db.blogPost.findMany({orderBy:{updatedAt:'desc'},include:{category:true},take:100});return <><PageHeader title="Blog posts" description="Published and draft content." action={<Link className="brand-gradient rounded-xl px-4 py-2 text-sm font-bold" href="/admin/blog/create">Create post</Link>}/><Card className="divide-y divide-[var(--line)]">{rows.map(x=><div key={x.id} className="flex flex-wrap justify-between gap-3 p-4 text-sm"><div><b>{x.title}</b><div className="muted text-xs">/{x.slug} · {x.category?.name??'Uncategorized'}</div></div><span>{x.published?'Published':'Draft'}</span></div>)}</Card></>}