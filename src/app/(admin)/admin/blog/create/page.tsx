import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { BlogCreateForm } from '@/components/admin/blog-create-form';export const dynamic='force-dynamic';export default async function Page(){const categories=await db.category.findMany({orderBy:{name:'asc'}});return <><PageHeader title="Create blog post" description="Add real database-backed blog content."/><BlogCreateForm categories={categories}/></>}