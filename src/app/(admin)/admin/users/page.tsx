import Link from 'next/link';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const users=await db.user.findMany({where:q?{OR:[{name:{contains:q,mode:'insensitive'}},{email:{contains:q,mode:'insensitive'}}]}:undefined,orderBy:{createdAt:'desc'},take:100});return <><PageHeader eyebrow="ADMIN" title="Users" description="Search and inspect user accounts."/><form className="mb-5"><input name="q" defaultValue={q} placeholder="Search name or email…" className="surface h-11 w-full max-w-md rounded-xl px-3 text-sm outline-none"/></form><Card className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead><tr className="border-b border-[var(--line)]"><th className="p-4">User</th><th>Role</th><th>Verified</th><th>Status</th><th>Created</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id} className="border-b border-[var(--line)] last:border-0"><td className="p-4"><b>{u.name}</b><div className="muted text-xs">{u.email}</div></td><td>{u.role}</td><td>{u.emailVerifiedAt?'Yes':'No'}</td><td>{u.suspendedAt?'Suspended':'Active'}</td><td>{u.createdAt.toLocaleDateString()}</td><td><Link className="font-bold text-[var(--brand)]" href={`/admin/users/${u.id}`}>View</Link></td></tr>)}</tbody></table></Card></>}