import Link from 'next/link';import { Card } from '@/components/ui/card';const plans=[['Free','$0','30 AI requests/day','Get started'],['Pro','$19','300 AI requests/day','Choose Pro'],['Business','$59','1,500 AI requests/day','Choose Business'],['Enterprise','Custom','Custom controls & volume','Contact sales']];export const metadata={title:'Pricing'};export default function Page(){return <main className="shell py-20"><div className="text-center"><h1 className="text-5xl font-black">Pricing that starts usable</h1><p className="muted mx-auto mt-4 max-w-xl">Limits are modeled in the database and can evolve without rewriting the product.</p></div><div className="mt-12 grid gap-4 lg:grid-cols-4">{plans.map(([name,price,limit,cta],i)=><Card key={name} className={`p-6 ${i===1?'ring-2 ring-[var(--brand)]':''}`}><div className="font-black">{name}</div><div className="my-5 text-4xl font-black">{price}<span className="muted text-sm">{price.startsWith('$')&&price!=='$0'?'/mo':''}</span></div><p className="muted text-sm">{limit}</p><ul className="muted my-6 space-y-2 text-sm"><li>✓ AI tools workspace</li><li>✓ Persistent conversations</li><li>✓ Secure account</li><li>✓ Usage visibility</li></ul><Link className="brand-gradient block rounded-xl px-4 py-3 text-center font-bold" href={name==='Enterprise'?'/contact':'/register'}>{cta}</Link></Card>)}</div><p className="muted mt-8 text-center text-sm">Payment checkout is intentionally not faked in this starter: connect your payment provider before enabling paid plan activation.</p></main>}