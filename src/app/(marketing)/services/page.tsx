import Link from 'next/link';import { Card } from '@/components/ui/card';import { services } from '@/data/marketing';export const metadata={title:'Services'};export default function Page(){return <main className="shell py-20"><h1 className="text-5xl font-black">Platform capabilities</h1><p className="muted mt-4 max-w-2xl text-lg">A real operational layer around AI workflows.</p><div className="mt-10 grid gap-5 md:grid-cols-3">{services.map(s=><Card key={s.slug} className="p-6"><h2 className="text-xl font-black">{s.title}</h2><p className="muted my-3 leading-7">{s.description}</p><Link className="font-bold text-[var(--brand)]" href={`/services/${s.slug}`}>View details →</Link></Card>)}</div></main>}