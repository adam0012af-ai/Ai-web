import { notFound } from 'next/navigation';import Link from 'next/link';import { services } from '@/data/marketing';export async function generateStaticParams(){return services.map(s=>({slug:s.slug}));}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <main className="shell py-20"><Link className="muted text-sm" href="/services">← All services</Link><h1 className="mt-6 text-5xl font-black">{s.title}</h1><p className="muted mt-5 max-w-3xl text-lg leading-8">{s.description}</p><div className="surface mt-10 max-w-3xl rounded-3xl p-7"><h2 className="text-2xl font-black">Designed to scale</h2><p className="muted mt-3 leading-7">This capability is connected to the same authentication, database, usage, security, and administration foundation as the rest of the platform instead of living as an isolated demo page.</p></div></main>}