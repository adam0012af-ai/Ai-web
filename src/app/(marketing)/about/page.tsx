import { Card } from '@/components/ui/card';export const metadata={title:'About'};export default function Page(){return <main className="shell py-20"><div className="max-w-3xl"><div className="text-sm font-black text-[var(--brand)]">ABOUT NEXA AI</div><h1 className="mt-3 text-5xl font-black">AI software should stay useful when infrastructure changes.</h1><p className="muted mt-6 text-lg leading-8">Nexa AI is designed as a modern SaaS product around a simple principle: product workflows should not be tightly coupled to one model provider. The platform combines a multi-provider AI layer with authentication, usage controls, support, billing architecture, notifications, content, and administration.</p></div><div className="mt-14 grid gap-5 md:grid-cols-3">{[['Reliability','Central routing, fallback and health state reduce provider coupling.'],['Control','Admin-managed limits, roles and provider settings keep operations visible.'],['Usability','A focused tool library and persistent workspace keep everyday tasks straightforward.']].map(([h,p])=><Card key={h} className="p-6"><h2 className="text-xl font-black">{h}</h2><p className="muted mt-2 leading-7">{p}</p></Card>)}</div></main>}