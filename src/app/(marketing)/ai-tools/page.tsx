import Link from 'next/link';import { Card } from '@/components/ui/card';import { aiTools } from '@/data/ai-tools';export const metadata={title:'AI Tools'};export default function Page(){return <main className="shell py-20"><h1 className="text-5xl font-black">25 practical AI tools</h1><p className="muted mt-4 max-w-2xl text-lg">Every tool uses the same server-side AI provider layer.</p><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{aiTools.map(t=><Card key={t.slug} className="p-5"><div className="flex items-center justify-between"><t.icon size={20}/><span className="muted text-xs">{t.category}</span></div><h2 className="mt-4 font-black">{t.title}</h2><p className="muted my-2 text-sm leading-6">{t.description}</p><Link className="font-bold text-[var(--brand)]" href={t.slug==='chat'?'/dashboard/ai/chat':`/dashboard/ai/${t.slug}`}>Launch →</Link></Card>)}</div></main>}