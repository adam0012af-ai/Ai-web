import { NextResponse } from 'next/server';import { z } from 'zod';import { getCurrentUser } from '@/lib/auth/session';import { verifyCsrf } from '@/lib/security/csrf';import { db } from '@/lib/db';const schema=z.object({enabled:z.boolean().optional(),priority:z.number().int().min(1).max(100).optional(),defaultModel:z.string().min(2).max(200).optional()});export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await verifyCsrf(req))return NextResponse.json({error:'Invalid request token'},{status:403});const u=await getCurrentUser();if(!u||u.role!=='SUPER_ADMIN')return NextResponse.json({error:'Super Admin required'},{status:403});const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid provider settings'},{status:400});const {id}=await params;await db.aIProviderConfig.update({where:{id},data:p.data});return NextResponse.json({ok:true});}