import { NextResponse } from 'next/server';import { getCurrentUser } from '@/lib/auth/session';import { verifyCsrf } from '@/lib/security/csrf';import { db } from '@/lib/db';export async function PATCH(req:Request){if(!await verifyCsrf(req))return NextResponse.json({error:'Invalid request token'},{status:403});const u=await getCurrentUser();if(!u)return NextResponse.json({error:'Unauthorized'},{status:401});const {action,id}=await req.json();if(action==='all-read')await db.notification.updateMany({where:{userId:u.id,readAt:null},data:{readAt:new Date()}});else if(action==='read'&&id)await db.notification.updateMany({where:{id,userId:u.id},data:{readAt:new Date()}});else if(action==='delete'&&id)await db.notification.deleteMany({where:{id,userId:u.id}});else return NextResponse.json({error:'Invalid action'},{status:400});return NextResponse.json({ok:true});}