import { NextResponse } from 'next/server';import { issueCsrf } from '@/lib/security/csrf';export async function GET(){return NextResponse.json({token:await issueCsrf()},{headers:{'cache-control':'no-store'}});}