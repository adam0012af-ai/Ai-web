import Link from 'next/link';import { AuthForm } from '@/components/auth/auth-form';import { AuthShell } from '@/components/auth/auth-shell';export default function Page(){return <AuthShell title="Create your account" subtitle="Start with the usable Free plan, then scale when you need to."><AuthForm mode="register"/><p className="muted mt-5 text-center text-sm">Already have an account? <Link className="font-bold text-[var(--brand)]" href="/login">Log in</Link></p></AuthShell>}