import { redirect } from 'next/navigation';import { getCurrentUser } from '@/lib/auth/session';import { OnboardingFlow } from '@/components/onboarding/onboarding-flow';export default async function Page(){const u=await getCurrentUser();if(!u)redirect('/login');if(u.onboardingCompletedAt)redirect('/dashboard');return <main className="grid min-h-screen place-items-center p-5"><OnboardingFlow/></main>}