import { Suspense } from 'react';import { AuthShell } from '@/components/auth/auth-shell';import { SimpleActionForm } from '@/components/auth/simple-action-form';export default function Page(){return <AuthShell title="Reset your password" subtitle="Enter your email. For security, the response does not reveal whether an account exists."><Suspense><SimpleActionForm kind="forgot"/></Suspense></AuthShell>}