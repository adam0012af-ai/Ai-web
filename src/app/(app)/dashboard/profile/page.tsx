import { getCurrentUser } from '@/lib/auth/session';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';import { ProfileForm } from '@/components/account/profile-form';export default async function Page(){const u=(await getCurrentUser())!;return <><PageHeader title="Profile" description="Manage the identity shown in your workspace."/><Card className="max-w-2xl p-6"><ProfileForm name={u.name} email={u.email}/></Card></>}
