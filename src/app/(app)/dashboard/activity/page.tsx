import { getCurrentUser } from '@/lib/auth/session';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const u=(await getCurrentUser())!;const rows=await db.activityLog.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'},take:100});return <><PageHeader title="Activity" description="Recent security and workspace activity."/><Card className="divide-y divide-[var(--line)]">{rows.length?rows.map(x=><div key={x.id} className="flex justify-between gap-4 p-4 text-sm"><b>{x.action}</b><span className="muted">{x.createdAt.toLocaleString()}</span></div>):<div className="muted p-8 text-center">Activity appears here as you use the platform.</div>}</Card></>}