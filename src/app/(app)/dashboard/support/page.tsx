import Link from 'next/link';import { getCurrentUser } from '@/lib/auth/session';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';import { Badge } from '@/components/ui/badge';import { TicketForm } from '@/components/support/ticket-form';export const dynamic='force-dynamic';export default async function Page(){const u=(await getCurrentUser())!;const tickets=await db.supportTicket.findMany({where:{userId:u.id},orderBy:{updatedAt:'desc'},take:50});return <><PageHeader title="Support" description="Create and track support tickets from your workspace."/><div className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><Card className="p-6"><h2 className="mb-4 text-lg font-black">Create ticket</h2><TicketForm/></Card><Card className="p-6"><h2 className="text-lg font-black">Your tickets</h2><div className="mt-3 divide-y divide-[var(--line)]">{tickets.length?tickets.map(t=><div key={t.id} className="py-4"><div className="flex flex-wrap items-center justify-between gap-2"><Link className="text-sm font-bold hover:text-[var(--brand)]" href={`/dashboard/support/${t.id}`}>{t.subject}</Link><div className="flex gap-2"><Badge>{t.priority}</Badge><Badge>{t.status}</Badge></div></div><p className="muted mt-2 text-sm line-clamp-2">{t.description}</p></div>):<p className="muted py-6 text-sm">No support tickets yet.</p>}</div></Card></div></>}