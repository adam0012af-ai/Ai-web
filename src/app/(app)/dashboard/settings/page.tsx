import { getCurrentUser } from '@/lib/auth/session';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';import { SettingsForm } from '@/components/account/settings-form';export const dynamic='force-dynamic';export default async function Page(){const u=(await getCurrentUser())!;const s=await db.userSettings.findUnique({where:{userId:u.id}})??{theme:'system',language:'en',emailNotifications:true,productUpdates:true};return <><PageHeader title="Settings" description="Workspace appearance and notification preferences."/><Card className="max-w-2xl p-6"><SettingsForm settings={s}/></Card></>}
