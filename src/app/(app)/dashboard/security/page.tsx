import { getCurrentUser } from '@/lib/auth/session';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';import { PasswordForm } from '@/components/account/password-form';export const dynamic='force-dynamic';export default async function Page(){const u=(await getCurrentUser())!;const sessions=await db.session.findMany({where:{userId:u.id,expiresAt:{gt:new Date()}},orderBy:{createdAt:'desc'}});return <><PageHeader title="Security" description="Change your password and review active sessions."/><div className="grid gap-6 xl:grid-cols-2"><Card className="p-6"><h2 className="mb-4 text-lg font-black">Change password</h2><PasswordForm/></Card><Card className="p-6"><h2 className="text-lg font-black">Active sessions</h2><div className="mt-3 divide-y divide-[var(--line)]">{sessions.map(s=><div className="py-3 text-sm" key={s.id}><b>{s.userAgent?.slice(0,70)??'Unknown browser'}</b><div className="muted mt-1 text-xs">{s.ipAddress||'Unknown IP'} · expires {s.expiresAt.toLocaleString()}</div></div>)}</div></Card></div></>}
