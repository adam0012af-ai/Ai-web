import { getCurrentUser } from '@/lib/auth/session';import { db } from '@/lib/db';import { PageHeader } from '@/components/dashboard/page-header';import { Card } from '@/components/ui/card';export const dynamic='force-dynamic';export default async function Page(){const u=(await getCurrentUser())!;const files=await db.uploadedFile.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'}});return <><PageHeader title="Files" description="Metadata and ownership are modeled now; connect S3/R2/Blob storage before enabling uploads."/><Card className="p-6">{files.length?files.map(f=><div key={f.id} className="border-b border-[var(--line)] py-3"><b>{f.name}</b><span className="muted ml-3 text-xs">{f.mimeType} · {(f.sizeBytes/1024).toFixed(1)} KB</span></div>):<div className="muted text-sm">No uploaded files. Storage upload controls are intentionally disabled until an object-storage provider is configured.</div>}</Card></>}