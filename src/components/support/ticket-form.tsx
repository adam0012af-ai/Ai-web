'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Textarea } from '@/components/ui/textarea';export function TicketForm(){const r=useRouter();const [busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const fd=new FormData(e.currentTarget);const csrf=await fetch('/api/csrf').then(x=>x.json());const res=await fetch('/api/support/tickets',{method:'POST',headers:{'content-type':'application/json','x-csrf-token':csrf.token},body:JSON.stringify(Object.fromEntries(fd.entries()))});setBusy(false);setMsg(res.ok?'Ticket created.':'Unable to create ticket.');if(res.ok){e.currentTarget.reset();r.refresh()}}return <form onSubmit={submit} className="space-y-3"><Input name="subject" placeholder="What do you need help with?" required/><select name="priority" className="h-11 w-full rounded-xl border border-[var(--line)] bg-transparent px-3 text-sm"><option value="LOW">Low</option><option value="MEDIUM">Medium</option><option value="HIGH">High</option><option value="URGENT">Urgent</option></select><Textarea name="description" placeholder="Describe the issue, what you expected, and what happened." required/><Button disabled={busy}>{busy?'Creating…':'Create ticket'}</Button>{msg&&<span className="muted ml-3 text-sm">{msg}</span>}</form>}