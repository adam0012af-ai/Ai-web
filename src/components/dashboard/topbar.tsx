import { Bell } from 'lucide-react';import Link from 'next/link';import { ThemeToggle } from '@/components/theme-toggle';import { getCurrentUser } from '@/lib/auth/session';import { CommandPalette } from './command-palette';import { MobileNav } from './mobile-nav';import { LogoutButton } from './logout-button';export async function Topbar(){const u=await getCurrentUser();return <header className="sticky top-0 z-30 flex h-17 items-center justify-between border-b border-[var(--line)] bg-[var(--bg)]/85 px-4 backdrop-blur-xl sm:px-6"><div className="flex items-center gap-2"><MobileNav/><CommandPalette/></div><div className="flex items-center gap-1"><Link className="relative rounded-xl p-2 hover:bg-black/[.04]" href="/dashboard/notifications"><Bell size={18}/></Link><ThemeToggle/><div className="hidden px-2 text-right sm:block"><div className="text-sm font-bold">{u?.name}</div><div className="muted text-[11px]">{u?.role}</div></div><div className="brand-gradient grid size-9 place-items-center rounded-full text-sm font-black">{u?.name?.[0]??'U'}</div><LogoutButton/></div></header>}