'use client';import { useState } from 'react';import Link from 'next/link';import { Menu,X } from 'lucide-react';import { Button } from '@/components/ui/button';import { dashboardNav } from './nav-items';export function MobileNav(){const [open,setOpen]=useState(false);return <><Button variant="ghost" className="lg:hidden" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu size={19}/></Button>{open&&<div className="fixed inset-0 z-50 bg-black/40 lg:hidden" onClick={()=>setOpen(false)}><aside className="h-full w-[82%] max-w-80 bg-[var(--card)] p-4" onClick={e=>e.stopPropagation()}><div className="mb-5 flex justify-between"><b>Navigation</b><Button variant="ghost" onClick={()=>setOpen(false)}><X size={18}/></Button></div>{dashboardNav.map(([label,href,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="muted flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold hover:bg-black/[.04]"><Icon size={17}/>{label}</Link>)}</aside></div>}</>}