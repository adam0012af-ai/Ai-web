import { Card } from '@/components/ui/card';
export function UsageChart({data}:{data:{label:string;value:number}[]}){const max=Math.max(1,...data.map(x=>x.value));return <Card className="p-6"><div className="flex items-end justify-between"><div><h2 className="text-lg font-black">7-day AI usage</h2><p className="muted mt-1 text-xs">Successful requests by day</p></div><span className="muted text-xs">{data.reduce((a,x)=>a+x.value,0)} total</span></div><div className="mt-7 flex h-44 items-end gap-2" aria-label="Seven day AI usage chart">{data.map(x=><div key={x.label} className="flex h-full flex-1 flex-col justify-end gap-2 text-center"><div className="relative flex-1"><div className="brand-gradient absolute bottom-0 left-1/2 w-[70%] max-w-10 -translate-x-1/2 rounded-t-lg transition-all" style={{height:`${Math.max(4,(x.value/max)*100)}%`}} title={`${x.label}: ${x.value}`}/></div><span className="muted text-[10px]">{x.label}</span></div>)}</div></Card>}
